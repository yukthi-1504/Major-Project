venv/
__pycache__/
*.pyc
.vscode/

# Ecolab network artifact - Dockerfile needs this to build behind Zscaler; harmless
# no-op elsewhere (public CA cert, not a secret), so it stays committed.
combined-ca-bundle.pem

.DS_Store
.claude/settings.local.json
